# PyCoin Trading Framework - Core Dependencies

# Required dependencies
aiohttp>=3.8.0              # Async HTTP client and WebSocket
pyyaml>=6.0                 # YAML configuration parser
logbook>=1.8.2              # Logging system

# Optional dependencies (install as needed)
redis>=4.5.0                # Redis cache (if using Redis features)
